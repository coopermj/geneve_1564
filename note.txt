## Session context — 2026-03-25/26

### What was done this session

1. **feature/net-notes branch created** — single-column notes variant
   - `net_notes.tex`: 179×239mm, left=18mm, right=55mm gutter (106mm text, ~59% width)
   - TikZ vertical rule at text/gutter boundary
   - `\renewcommand{\twocolumn}[1][]{#1}` + `\renewcommand{\onecolumn}{}` — neutralize shared livres/ \twocolumn calls
   - `\begin{adjustwidth}{0pt}{-40mm}` for full-width TOC (avoids \newgeometry blank-page)
   - livres/color_index.tex: rebalanced 3-col breaks + vspace 4pt→1pt to fit on one page
   - Removed bigfoot \DeclareNewFootnote (caused "main[alph]" text on page 1)
   - Cover: wisdom green, gutter note-lines, accent strips

2. **Cover pages** — unique design per edition
   - master/ESV (livres_esv/cover.tex): gospels blue, double-border frame, diamond corners
   - feature/net-notes (livres/cover.tex): wisdom green, horizontal note-ruled gutter, accent strips
   - feature/geneva-annotations (livres/cover.tex): majorprophets purple, L-bracket corners, faint Geneva annotation text
   - Pattern: `\onecolumn` in main .tex BEFORE \input{cover}; `\clearpage` at END of cover.tex

3. **Word-spacing fix** (master + geneva-annotations)
   - `<p>` tags replaced with ' ' instead of '' in _strip_html_tags() — prevents "outand"/"mebecause" merges

4. **Geneva annotations overlap convergence** (feature/geneva-annotations)
   - Fixed overlap_detector.py: aux-based page assignment + content-based manifest matching
   - build_annotated.py run: density-prune (2 passes) + 3 iter + 5 iter = 554 corrections
   - Third convergence run in progress (started 2026-03-26)

### Next tasks
- Verify geneva-annotations converges to 0 overlaps (third run in progress)
- Commit and push final corrections_final.json once converged
